/**
 * Demo POS: credentials required on the Orders page before an order can be deleted.
 * Change these values for your deployment; there is no server-side validation in this demo.
 */
export const ORDER_DELETE_AUTH = {
  username: "admin",
  password: "admin123",
} as const
